# Relay account theme — inherits keycloak's classic FTL account console
# (the simpler one to override via CSS). If we move to keycloak.v2 account
# (the React SPA), customizing requires ejecting + rebuilding the SPA;
# parent=keycloak keeps it pure-CSS.
parent=keycloak
import=common/keycloak

styles=css/styles.css
